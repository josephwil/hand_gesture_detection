{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Python: Gesture Recognition",
            "type": "python",
            "request": "launch",
            "program": "${workspaceFolder}/gesture_recognition.py",
            "console": "integratedTerminal",
            "justMyCode": true,
            "env": {
                "PYTHONPATH": "${workspaceFolder}"
            }
        }
    ]
}